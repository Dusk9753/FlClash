import 'package:flutter/material.dart';

class SystemProfileEmptyState extends StatelessWidget {
  const SystemProfileEmptyState({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
